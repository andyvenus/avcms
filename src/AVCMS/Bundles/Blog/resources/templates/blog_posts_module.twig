<div class="list-group">
    {% for post in posts %}
        <a href="{{ path('blog_post', {slug: post.slug}) }}" class="list-group-item">
            {{ post.title }} <br/>
            {% if admin_settings.show_date %}
                <span class="small"><span class="glyphicon glyphicon-calendar"></span> {{ post.publishDate|date(settings.date_format) }}</span>
            {% endif %}
        </a>
    {% else %}
        <div class="list-group-item">{% trans %}No blog posts{% endtrans %}</div>
    {% endfor %}
</div>
