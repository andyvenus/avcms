<span class="user-info">
    {% if info_user.id != null %}

        <span class="username">
            {% if settings.users_enabled %}
                <img class="avatar" src="{{ avatar_url(info_user) }}" width="{{ options.avatar_width }}" height="{{ options.avatar_height }}"/> <a href="{{ url('user_profile', {slug: info_user.slug}) }}">{{ info_user.username }}</a>
            {% else %}
                {{ info_user.username }}
            {% endif %}
        </span>

    {% else %}
        <span class="username">{% trans %}Anonymous{% endtrans %}</span>
    {% endif %}
</span>