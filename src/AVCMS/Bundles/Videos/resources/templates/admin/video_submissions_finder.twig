{% extends '@admin/finder.twig' %}

{% block finder_header_titles %}
    <div class="col-md-2 hidden-xs">
        {% trans %}User{% endtrans %}
    </div>
    <div class="col-md-2 hidden-sm hidden-xs">
        {% trans %}Date submitted{% endtrans %}
    </div>
    <div class="col-md-8 hidden-sm hidden-xs">
        {% trans %}File{% endtrans %}
    </div>
{% endblock %}

{% block item_main %}
    <div class="finder-thumbnail">
        <img src="{{ video_thumbnail(item) }}" width="50" height="50"/>
    </div>

    {% block item_title %}
        <div class="finder-item-title" data-field="name">
            {{ item.name }}
        </div>
    {% endblock %}

    {% block item_options %}
        <div class="btn-group">
            <a class="btn btn-xs btn-default" href="{{ path('video_submissions_admin_review', {'id': item.id}) }}">
                <span class="glyphicon glyphicon-ok"></span> {% trans %}Review{% endtrans %}
            </a>

            {{ buttons.delete('video_submissions_admin_delete') }}
        </div>

        <a href="{{ path('video_submissions_admin_play', {'id': item.id}) }}" class="btn btn-xs btn-default"><span class="glyphicon glyphicon-play"></span> {% trans %}Play{% endtrans %}</a>

    {% endblock %}
{% endblock %}

{% block item_extended_inner %}
    <div class="col-md-2 hidden-xs">
        <a href="{{ path('users_admin_edit', {'id': item.user.id}) }}">{{ item.user.username }}</a>
    </div>
    <div class="col-md-2 hidden-sm hidden-xs">
        {{ item.dateAdded|date(settings.date_format) }}
    </div>
    <div class="col-md-8 hidden-sm hidden-xs">
        {{ item.file }}
    </div>
{% endblock %}
