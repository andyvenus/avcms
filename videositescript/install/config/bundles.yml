Framework:
  enabled: true
Model:
  enabled: true
  config:
    container_params:
      database_config_location: webmaster/config/database.php
Twig:
  enabled: true
Form:
  enabled: true
Installer:
  enabled: true
  config:
    container_params:
      main_app_dir: videositescript
